import 'package:flutter/material.dart';

class ConfirmAccountPin extends StatelessWidget {
  const ConfirmAccountPin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}